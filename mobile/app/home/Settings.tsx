import { Text } from "react-native";
import Screen from "@/components/Screen";

function Settings() {
  return (
    <Screen>
      <Text>settings</Text>
    </Screen>
  );
}

export default Settings;